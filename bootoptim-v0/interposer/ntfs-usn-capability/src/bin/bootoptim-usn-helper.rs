#![cfg_attr(windows, windows_subsystem = "windows")]
#[cfg(not(windows))] fn main(){std::process::exit(2)}

#[cfg(windows)] mod windows_helper {
 use bootoptim_ntfs_usn_capability::{decode_lower_hex,protocol::{self,Handshake,Request,RequestKind,Response,ResponseStatus}};
 use std::{ffi::{c_void,OsStr},mem::{size_of,zeroed},os::windows::ffi::OsStrExt,ptr::{null,null_mut},time::{Duration,Instant}};
 type Handle=*mut c_void; const INVALID_HANDLE_VALUE:Handle=-1isize as Handle; const GENERIC_READ:u32=0x8000_0000; const GENERIC_WRITE:u32=0x4000_0000; const FILE_READ_ATTRIBUTES:u32=0x80;
 const FILE_SHARE_READ:u32=1;const FILE_SHARE_WRITE:u32=2;const FILE_SHARE_DELETE:u32=4;const OPEN_EXISTING:u32=3;const FILE_FLAG_OPEN_REPARSE_POINT:u32=0x0020_0000;const PIPE_NOWAIT:u32=1;
 const ERROR_NO_DATA:i32=232;const ERROR_PIPE_LISTENING:i32=536;const FSCTL_QUERY_USN_JOURNAL:u32=0x0009_00f4;const FSCTL_READ_FILE_USN_DATA:u32=0x0009_00eb;const FILE_ATTRIBUTE_DIRECTORY:u32=0x10;const FILE_ATTRIBUTE_REPARSE_POINT:u32=0x400;const IO_TIMEOUT:Duration=Duration::from_secs(5);
 #[repr(C)]#[derive(Clone,Copy)]struct Info{file_attributes:u32,creation_time_low:u32,creation_time_high:u32,last_access_time_low:u32,last_access_time_high:u32,last_write_time_low:u32,last_write_time_high:u32,volume_serial_number:u32,file_size_high:u32,file_size_low:u32,number_of_links:u32,file_index_high:u32,file_index_low:u32}
 #[repr(C)]union FileIdUnion{file_id:i64,padding:[u8;16]} #[repr(C)]struct FileIdDescriptor{size:u32,id_type:i32,id:FileIdUnion}
 #[link(name="kernel32")]unsafe extern "system"{fn CreateFileW(n:*const u16,a:u32,s:u32,sec:*const c_void,c:u32,f:u32,t:Handle)->Handle;fn CloseHandle(h:Handle)->i32;fn ReadFile(h:Handle,b:*mut c_void,l:u32,r:*mut u32,o:*mut c_void)->i32;fn WriteFile(h:Handle,b:*const c_void,l:u32,w:*mut u32,o:*mut c_void)->i32;fn PeekNamedPipe(h:Handle,b:*mut c_void,l:u32,r:*mut u32,a:*mut u32,left:*mut u32)->i32;fn SetNamedPipeHandleState(h:Handle,m:*mut u32,c:*mut u32,t:*mut u32)->i32;fn GetNamedPipeServerProcessId(h:Handle,p:*mut u32)->i32;fn GetCurrentProcessId()->u32;fn GetFileInformationByHandle(h:Handle,i:*mut Info)->i32;fn GetVolumeInformationByHandleW(h:Handle,n:*mut u16,nl:u32,s:*mut u32,m:*mut u32,f:*mut u32,fs:*mut u16,fsl:u32)->i32;fn DeviceIoControl(h:Handle,c:u32,ib:*const c_void,il:u32,ob:*mut c_void,ol:u32,r:*mut u32,o:*mut c_void)->i32;fn OpenFileById(v:Handle,d:*const FileIdDescriptor,a:u32,s:u32,sec:*const c_void,f:u32)->Handle;}
 struct Owned(Handle);impl Owned{fn new(h:Handle)->Option<Self>{(!h.is_null()&&h!=INVALID_HANDLE_VALUE).then_some(Self(h))}}impl Drop for Owned{fn drop(&mut self){unsafe{CloseHandle(self.0);}}}
 fn wide(v:&OsStr)->Vec<u16>{v.encode_wide().chain(Some(0)).collect()} fn last_error()->i32{std::io::Error::last_os_error().raw_os_error().unwrap_or(-1)}
 fn read_exact(h:Handle,o:&mut[u8],deadline:Instant)->bool{while Instant::now()<deadline{let mut a=0;let ok=unsafe{PeekNamedPipe(h,null_mut(),0,null_mut(),&mut a,null_mut())};if ok==0{if matches!(last_error(),ERROR_NO_DATA|ERROR_PIPE_LISTENING){std::thread::sleep(Duration::from_millis(5));continue}return false}if a<o.len() as u32{std::thread::sleep(Duration::from_millis(5));continue}let mut r=0;return unsafe{ReadFile(h,o.as_mut_ptr().cast(),o.len() as u32,&mut r,null_mut())}!=0&&r as usize==o.len()}false}
 fn write_exact(h:Handle,i:&[u8])->bool{let mut w=0;unsafe{WriteFile(h,i.as_ptr().cast(),i.len() as u32,&mut w,null_mut())}!=0&&w as usize==i.len()}
 fn file_id(i:&Info)->[u8;16]{let v=((i.file_index_high as u64)<<32)|i.file_index_low as u64;let mut o=[0;16];o[..8].copy_from_slice(&v.to_le_bytes());o}
 fn open_volume(guid:&str)->Option<(Owned,u64)>{let p=wide(OsStr::new(guid.strip_suffix('\\')?));let h=Owned::new(unsafe{CreateFileW(p.as_ptr(),GENERIC_READ,FILE_SHARE_READ|FILE_SHARE_WRITE|FILE_SHARE_DELETE,null(),OPEN_EXISTING,0,null_mut())})?;let mut serial=0;let mut max=0;let mut flags=0;let mut fs=[0u16;16];if unsafe{GetVolumeInformationByHandleW(h.0,null_mut(),0,&mut serial,&mut max,&mut flags,fs.as_mut_ptr(),fs.len() as u32)}==0{return None}let n=fs.iter().position(|v|*v==0).unwrap_or(fs.len());if String::from_utf16_lossy(&fs[..n])!="NTFS"{return None}Some((h,serial as u64))}
 fn journal(h:Handle)->Option<protocol::JournalState>{let mut o=[0u8;128];let mut r=0;if unsafe{DeviceIoControl(h,FSCTL_QUERY_USN_JOURNAL,null(),0,o.as_mut_ptr().cast(),o.len() as u32,&mut r,null_mut())}==0||r<32{return None}Some((u64::from_le_bytes(o[0..8].try_into().ok()?),i64::from_le_bytes(o[8..16].try_into().ok()?),i64::from_le_bytes(o[24..32].try_into().ok()?),i64::from_le_bytes(o[16..24].try_into().ok()?)))}
 fn file_usn(volume:Handle,id:[u8;16])->Option<i64>{if id[8..].iter().any(|b|*b!=0){return None}let raw=i64::from_le_bytes(id[..8].try_into().ok()?);let d=FileIdDescriptor{size:size_of::<FileIdDescriptor>() as u32,id_type:0,id:FileIdUnion{file_id:raw}};let f=Owned::new(unsafe{OpenFileById(volume,&d,FILE_READ_ATTRIBUTES,FILE_SHARE_READ|FILE_SHARE_WRITE|FILE_SHARE_DELETE,null(),FILE_FLAG_OPEN_REPARSE_POINT)})?;let mut info:Info=unsafe{zeroed()};if unsafe{GetFileInformationByHandle(f.0,&mut info)}==0||info.file_attributes&(FILE_ATTRIBUTE_DIRECTORY|FILE_ATTRIBUTE_REPARSE_POINT)!=0||file_id(&info)!=id{return None}let mut o=[0u8;256];let mut r=0;if unsafe{DeviceIoControl(f.0,FSCTL_READ_FILE_USN_DATA,null(),0,o.as_mut_ptr().cast(),o.len() as u32,&mut r,null_mut())}==0||r<32||u16::from_le_bytes([o[4],o[5]])!=2{return None}Some(i64::from_le_bytes(o[24..32].try_into().ok()?))}
 fn query(q:Request)->Response{let mut r=Response{status:ResponseStatus::InvalidRequest,nonce:q.nonce,volume_serial:0,journal_id:0,first_usn:0,lowest_valid_usn:0,next_usn:0,file_id:q.file_id,file_usn:0};let Some(guid)=protocol::volume_guid_str(&q.volume_guid)else{return r};let Some((volume,serial))=open_volume(guid)else{r.status=ResponseStatus::NonNtfs;return r};let Some(before)=journal(volume.0)else{r.status=ResponseStatus::IoError;return r};if !protocol::journal_state_is_valid(before){r.status=ResponseStatus::IoError;return r}let mut cur=before;r.volume_serial=serial;if q.kind==RequestKind::File{let Some(usn)=file_usn(volume.0,q.file_id)else{r.status=ResponseStatus::NotFound;return r};let Some(after)=journal(volume.0)else{r.status=ResponseStatus::IoError;return r};if !protocol::journal_transition_is_consistent(before,after){r.status=ResponseStatus::IoError;return r}cur=after;r.file_usn=usn}r.journal_id=cur.0;r.first_usn=cur.1;r.lowest_valid_usn=cur.2;r.next_usn=cur.3;r.status=ResponseStatus::Ok;r}
 pub fn run()->i32{let mut pipe=None;let mut nonce=None;let mut server=None;let mut args=std::env::args_os().skip(1);while let Some(a)=args.next(){match a.to_str(){Some("--pipe")if pipe.is_none()=>pipe=args.next(),Some("--nonce")if nonce.is_none()=>nonce=args.next(),Some("--server-pid")if server.is_none()=>server=args.next(),_=>return 2}}let Some(pipe)=pipe.and_then(|v|v.into_string().ok())else{return 2};let Some(nt)=nonce.and_then(|v|v.into_string().ok())else{return 2};let Some(nonce)=decode_lower_hex::<{protocol::NONCE_LEN}>(&nt)else{return 2};if pipe!=format!(r"\\.\pipe\BootOptimPandora-USN-v1-{nt}"){return 2}let Some(server)=server.and_then(|v|v.to_str().and_then(|s|s.parse::<u32>().ok()))else{return 2};if server==0{return 2}
  let pw=wide(OsStr::new(&pipe));let Some(ph)=Owned::new(unsafe{CreateFileW(pw.as_ptr(),GENERIC_READ|GENERIC_WRITE,0,null(),OPEN_EXISTING,0,null_mut())})else{return 3};let mut actual=0;if unsafe{GetNamedPipeServerProcessId(ph.0,&mut actual)}==0||actual!=server{return 4}let mut mode=PIPE_NOWAIT;if unsafe{SetNamedPipeHandleState(ph.0,&mut mode,null_mut(),null_mut())}==0{return 4}let mut hb=[0u8;protocol::HANDSHAKE_LEN];if !read_exact(ph.0,&mut hb,Instant::now()+IO_TIMEOUT){return 5}let Some(h)=protocol::decode_handshake(&hb)else{return 4};if h.nonce!=nonce||h.pid!=server{return 4}let ack=protocol::encode_handshake(Handshake{nonce,pid:unsafe{GetCurrentProcessId()}});if !write_exact(ph.0,&ack){return 5}
  let mut session_volume:Option<[u8;protocol::VOLUME_GUID_LEN]>=None;loop{let mut rb=[0u8;protocol::REQUEST_LEN];if !read_exact(ph.0,&mut rb,Instant::now()+IO_TIMEOUT){return 5}let Some(q)=protocol::decode_request(&rb)else{return 4};if q.nonce!=nonce{return 4}if q.kind==RequestKind::Shutdown{return 0}match session_volume{None=>session_volume=Some(q.volume_guid),Some(v)if v!=q.volume_guid=>return 4,_=>{}}let out=protocol::encode_response(query(q));if !write_exact(ph.0,&out){return 5}}
 }
}
#[cfg(windows)] fn main(){std::process::exit(windows_helper::run())}
